interface:
  display_name: "Issue Delivery"
  short_description: "Ship issues with one-gate verified closeout"
  default_prompt: "Use $issue-delivery to implement this issue in reviewed steps, delegate body writes to $issue-pr-body-contract, and treat an unambiguous ready-to-merge instruction as compound authorization to merge the exact verified head, complete verified closeout, and perform safe cleanup."
